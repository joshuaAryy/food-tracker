# Pre-Phase-24 Regression Defect Ledger

This ledger is the execution companion to the acceptance matrix. It remains
empty of defects until a scenario is actually reproduced. Every entry must
contain exact reproduction, evidence, severity, failing layer, falsifiable
root-cause hypothesis, regression coverage, targeted and neighboring results,
Simulator re-run, persistence/downstream verification, and focused commit.

| ID | Scenario | Reproduction/evidence | Severity | Failing layer | Root-cause hypothesis | Regression test | Fix/commit | Re-run evidence | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| — | No additional verified defects recorded beyond the entries below. | — | — | — | — | — | — | — | INFO |
| AI-SERVING-001 | AI text review could not recover a parsed quantity when the unit was missing | QA A real iOS 27 Simulator entered `large chicken shawarma plate`. The review returned one editable `chicken shawarma` row with a trusted mass-basis candidate, amount `1`, blank unit, and visible `Use g/kg/oz/lb` controls. Selecting `Use g` left the row in `Check this serving` with `Log selected` disabled because the mobile handler attempted conversion from the blank unit instead of assigning the selected supported unit. Editing the amount alone did not make the row loggable. | P1 | Mobile AI meal review serving-choice state transition | `meal-describe.tsx` converted every selected choice through `convertServingAmountForUnitChange`; `classifyServingUnit('')` is incompatible, so incomplete parsed serving state could never transition to a supported unit. | `apps/mobile/src/lib/ai-serving.test.ts` fails before the correction and asserts that a blank-unit parsed quantity becomes a valid mass-unit state; full mobile Vitest now passes 67 files / 444 tests. | `d35eb09` | Rebuilt with Xcode beta and installed the exact candidate on QA A Simulator. Repeated the same phrase: `Use g` selected, preview became `2 kcal · 0.3 g protein` at `1.0 g`, amount `200` produced `376 kcal / 52.2 g protein` at `200.0 g`, the row was checkable, and Close returned to Log food with no saved entry. Mobile typecheck/lint passed. | FIXED-SIMULATOR |
| GOAL-PACE-001 | Profile goal editing exposed stale categorical pace controls and allowed a missing rate for Lose/Gain | QA A real Simulator Profile → Edit goals reproduction showed `None`, `Slow`, `Moderate`, `Aggressive`, and bulk categorical choices after selecting Lose. Source tracing found the profile editor rendered legacy `GOAL_PACES` for every goal type and submitted `values.goalPace` directly while treating the numeric rate as optional. The canonical contract requires a numeric 0.50–2.00 lb/week rate in 0.05 steps for Lose/Gain and null only for Maintain. | P1 | Mobile Profile goal-editor validation/save boundary | Profile retained a legacy compatibility enum as a user-facing control even though onboarding and the shared policy use numeric rates; save did not derive compatibility from the authoritative numeric rate. | `profile-goal-pace.test.tsx` failed before correction because `Slow` remained exposed and passes after correction; mobile typecheck/lint and full Jest (69 suites / 208 tests) pass. | `63ece53` | QA A Simulator showed only the numeric `Weekly rate (lb/week)` field for Lose, no categorical pace choices, and blank-rate validation. Entering `0.55`, saving, terminating/relaunching, and reopening Profile showed Lose / `0.55 lb/week`; Progress showed the propagated `2,320 kcal` target. | FIXED-SIMULATOR |
| GOAL-PACE-002 | Switching a saved loss plan to Maintain retained and submitted the previous weekly rate | QA A Profile → Edit goals selected Maintain while the prior Lose rate was `0.55`. The form still displayed `0.55`; Save was blocked by the Maintain validation error, and source tracing showed the save handler would otherwise submit the stale numeric rate instead of canonical `null`. | P1 | Mobile Profile goal-editor state/save boundary | Goal-type changes did not clear the incompatible rate field, and the save boundary parsed the stale value without treating Maintain as null. | `profile-goal-pace.test.tsx` now fails before correction when Maintain is selected and asserts `targetRateLbPerWeek: null`; focused suite passes after correction, alongside mobile Vitest 67 files / 444 tests, typecheck, and lint. | `f658b30` | QA A Simulator selected Maintain, observed the rate field clear and `No pace`, saved, observed 2,600 kcal Maintain targets, then restored Lose / 0.55 lb/week and 2,320 kcal for the primary fixture. | FIXED-SIMULATOR |
| ACCOUNT-DELETE-001 | Disposable account deletion stopped at a recent-auth error with no reauthentication path | QA C was verified by the recognizable `testacct4659` profile and previously verified Firebase UID, reached the real Profile → Delete account confirmation, entered `DELETE`, and tapped the destructive action. The API correctly returned `RECENT_AUTH_REQUIRED`; the panel only rendered the error and offered no way to reauthenticate, leaving the account-deletion flow unusable. | P1 | Mobile account-deletion recovery / Firebase recent-auth boundary | `DeleteAccountPanel` received only `deleteAccount`, despite `AuthRuntimeContext` already exposing password and Google reauthentication actions. The UI could not satisfy the server's recent-auth requirement. | `apps/mobile/src/components/auth/__tests__/account-deletion.test.tsx` now reproduces the recent-auth response, requires a current-password field, invokes reauthentication, and retries deletion; it failed before correction and passes after correction. | `a2bde6c` | After the fix, the real QA C Simulator exposed `Current password` and `Verify identity` after the same recent-auth response. Entering the transient QA password and verifying identity retried deletion; the app signed out to the signed-out screen. No A/B account was touched. | FIXED-SIMULATOR |
| NUTRIENT-EDIT-001 | Complex normalized nutrient override was not visible after reopening a FoodLog | QA A Complex History: after setting `Added Sugar = 2 g` on `QA Archive Probe`, saving, leaving the editor, and reopening it through the real Simulator History route, the live accessibility state now shows `Added Sugar (g)` value `2`. A direct authenticated staging readback for the same FoodLog confirms `nutrients.addedSugar.amount=2` and `servingSnapshot.nutritionOverride.nutrients.addedSugar.amount=2`. | P1 | Mobile FoodLog edit-state / snapshot boundary | The serving-preview effect rebuilt Complex nutrient fields from immutable `basisNutrition` after `loadForm` had hydrated the persisted override, clobbering the override in local state. The minimal correction preserves snapshot overrides when a serving preview arrives, while still using preview nutrients when no override exists or it was explicitly cleared. | `apps/mobile/src/lib/food-log-nutrient-state.test.ts` now has 3 tests, including a regression proving a preview cannot clobber a persisted override; focused Vitest, full mobile Vitest (66 files / 442 tests), full mobile Jest (68 suites / 207 tests), mobile typecheck, and mobile lint pass. | `dd68b9e` | Real Simulator edit → save → leave → reopen now shows value `2`; API readback and UI agree. Numeric/zero/Unknown authoritative-serving probe remains preserved. | FIXED-SIMULATOR |
| SNAPSHOT-MACRO-001 | Serving preview could replace persisted top-level macro overrides during a later FoodLog edit | QA A History → Friday, September 11 → `QA Archive Probe` exposed a persisted snapshot showing 172 kcal, 1.7 g protein, 38.6 g carbs, and 1.4 g fat while the serving preview showed the immutable 10 kcal / 1.0 g protein basis. Source tracing found the serving-preview effect unconditionally copied preview top-level nutrition into the form; any subsequent normalized-nutrient edit could therefore submit those basis values through `nutritionOverrideFromValues()` and rewrite the historical macros. | P1 | Mobile FoodLog edit-state / snapshot nutrition boundary | The preview effect did not distinguish an existing persisted top-level `nutritionOverride` from a basis-only preview. The correction preserves current form macros while an override exists, and only adopts preview macros for a basis-only log or after the user explicitly clears the adjustment. | `apps/mobile/src/lib/food-log-nutrient-state.test.ts` adds a red/green regression for persisted macro preservation (4 focused tests total); the intentionally inverted helper produced the expected single failure before restoration. Full mobile Vitest (66 files / 443 tests), full mobile Jest (69 suites / 209 tests), mobile typecheck, lint, targeted formatting, and `git diff --check` pass. | `ce09db2` | Fresh beta-built QA A Simulator opened QA Archive Probe, changed serving 100 g→200 g, entered Added Sugar 2 g, saved through the off-screen Save changes action, reopened the entry with the 200 g preview and Added Sugar 2 persisted, then removed the adjustment, restored 100 g, saved, and confirmed Friday totals/fixture row were restored. | FIXED-SIMULATOR |
| LIB-DETAIL-001 | Food Library manual-food detail remained stale after returning from an edit | QA A fresh beta-built iOS 27 Simulator opened Food Library → My Foods → `QA Archive Probe` (10 kcal), edited its canonical Calories to 20 and saved. The server-backed library list later showed 20 kcal, but the detail route initially remained mounted with the old 10 kcal state after returning from `manual-food`, so the user could be shown stale nutrition until leaving and reopening the detail. | P2 | Mobile Food Library detail focus/reload boundary | `FoodLibraryDetailScreen` loaded only on initial mount. Returning from the nested manual-food editor did not trigger a canonical reload, while the editor correctly called `markDataChanged()` for list consumers. | `apps/mobile/src/app/food-log/__tests__/library-detail.test.tsx` first failed because no focus reload callback was registered, then passed after asserting a second `libraryDetail` request when focus returns. | `1474a76` | Rebuilt with Xcode beta and installed the exact candidate. The real QA A Simulator changed 10→20 kcal and, on return from the editor, the detail immediately displayed 20 kcal; the canonical food was then restored to 10 kcal and the fresh library list/detail verified the fixture value. | FIXED-SIMULATOR |
| AI-RAG-001 | Text AI accepted deterministic retrieval without post-retrieval adequacy judgment | The first request-boundary regression run expected a parse request plus a bounded candidate-evaluation request, but observed only one Gemini request; the route's provider interface exposed parse only and `retrieveParsedFoodItems` output was returned directly. | P1 | API AI text orchestration | Parse and retrieval were separate, but no provider/evaluation boundary existed to classify excellent, review-required, or fallback-eligible candidates. | `ai-food-parse.test.ts` now proves candidate evidence is sent, an ambiguous candidate becomes `needs_review`, and invalid/incomplete evaluation cannot preserve a trusted result; neighboring AI/serving tests pass. | `c73d018`, `697236a`, `cb51c62` | Full API suite 116 files / 1,401 tests green. QA A Simulator evidence now covers the named `1 apple` recovery, multi-food trusted/review rows, partial unresolved-component review, and opaque serving-required review states; no fallback or trusted row was silently persisted without user resolution. | FIXED-AUTOMATED |
| AI-KEYBOARD-001 | Meal-description keyboard hid the required Read meal action and left the text flow unusable | QA A Fresh iOS 27 Simulator reproduced the defect on the real Describe meal route: focusing the multiline field opened the software keyboard over the lower form, and the required `Read meal` footer could not be activated. The short form could not be dragged far enough for a scroll-only dismissal. After the targeted correction was installed, the Simulator showed a `Done` keyboard action; tapping `Done` dismissed the keyboard, the footer remained reachable, and tapping `Read meal` transitioned to the editable Review foods state with serving controls. No log was saved. | P1 | Mobile meal-description keyboard/recovery interaction | `AppScreen` exposed no keyboard dismissal policy and the short non-scrollable form gave the user no reliable way to dismiss the multiline keyboard. The minimal correction passes `keyboardDismissMode="on-drag"` through `AppScreen` and configures the meal-description input with `blurOnSubmit`, `returnKeyType="done"`, and `Keyboard.dismiss` so the required footer can be reached. | `apps/mobile/src/components/__tests__/app-screen.test.tsx` fails before the prop correction and now passes; full mobile Jest is 71 suites / 212 tests, plus mobile typecheck/lint. The real UI interaction is the required interaction-level regression evidence. | `1a52472` | Xcode-beta incremental build completed with `** BUILD SUCCEEDED **`; exact bundle installed on Fresh QA `A716FD01-3A7D-4D5C-90D7-71C139F9EAD1`. Real route was opened, example text selected, multiline keyboard opened, `Done` dismissed it, `Read meal` was activated, and Review foods rendered with an editable `eggs` row, candidate choices, serving amount, units, and `Log selected`. The original `1 apple` path remains separately named and previously evidenced; no mutation was performed in this rerun. | FIXED-SIMULATOR |
| AI-PARTIAL-001 | Partial-fallback meal silently omitted an explicit unresolved component | An initial QA A real Simulator Describe meal observation appeared to show only chicken, rice, and peas for `chicken, rice, peas, mystery house sauce`. A direct authenticated staging request for the identical description returned four items, with `mystery house sauce` present as `reviewStatus=unmatched`, `loggable=false`, and no selected candidate. A fresh complete UI scroll then showed all four rows. | P1 (initially suspected) | Evidence/viewport interpretation (not a product defect) | The first hypothesis was server-side component loss; request-boundary evidence and complete UI rendering both preserve the unresolved component. The apparent omission was caused by treating a partial viewport as the complete review state. | Retain the request-boundary regression asserting all four named components survive as matched, review-required, or unmatched/fallback-eligible, plus the named full-scroll Simulator journey. | — | Fresh QA A Simulator evidence showed chicken, rice, and peas as checked editable rows and the sauce as a separate unchecked `Needs food` row with remove/review controls; `Log selected` remained scoped to resolved rows and no meal was saved. | CLOSED-NOT-REPRODUCED |
| AUTH-BOOTSTRAP-001 | Auth session cleanup rejected after a stale token refresh | On the staging-target Simulator, Firebase reported no current user, then a forced token refresh produced `auth_initialization_failed` followed by visible `Uncaught (in promise) AuthServiceError: Authentication failed.` Metro logs and the Simulator screenshot captured the failure. A focused Jest regression failed before the fix because the API-session cleanup callback fired `authService.signOut()` without consuming its rejection. | P1 | Mobile auth bootstrap/API-session boundary | The API client correctly treated token failure as signed-out, but AuthBootstrap supplied a fire-and-forget Firebase sign-out callback; a rejected sign-out escaped the promise chain and surfaced as an uncaught runtime error. | `apps/mobile/src/components/auth/__tests__/auth-bootstrap.test.tsx` reproduces a rejected `AuthServiceError` and asserts no `unhandledRejection`; it failed before the fix and passes after it. Neighboring auth bootstrap and API-session tests pass. | `22d73d7` | Fresh staging Metro/Simulator relaunch reached signed-out `Welcome back`; the same forced-refresh diagnostic was handled without a new `AuthServiceError` or uncaught overlay. The remaining Expo-notifications entitlement warning is excluded scope. | FIXED |
| MODE-ICON-001 | Mode switch surfaced a launcher-icon failure as a Progress error after the mode save succeeded | On QA A iOS 27 Simulator, tap the Complex/Simple mode control, wait for the Progress screen, and observe `Couldn’t refresh progress` with `Tracking mode was saved, but the launcher icon could not be updated. The request could not be completed.`. The tracking-preferences API readback changed to the requested mode and LaunchServices logs showed the preferred icon operation, so the primary mode mutation succeeded while the secondary icon promise rejected. | P2 | Mobile Progress mode toggle / non-critical native side effect | `progress.tsx` treated a rejected `syncLauncherIconToMode` promise as the screen's primary load error even though the mode update and data refresh had completed. | `apps/mobile/src/app/(tabs)/__tests__/progress-regressions.test.tsx` now forces icon sync rejection after a successful mode update and asserts the mode label changes without rendering `Couldn’t refresh progress`; it failed before the fix and passes after it. | `36d34d6` | Re-ran QA A through the real Simulator after the focused change: Complex → Simple → Complex labels and API mode readback remained correct and no Progress error state appeared. The icon rejection remains diagnostic-only. | FIXED |
| LIB-REUSE-001 | History offered an impossible Save to My Foods action for an unoverridden provider FoodLog | QA A real Simulator History showed `Save Apple, raw to My Foods` for a provider-backed log. Confirming the action reached `/api/v1/food-logs/f1023dae-78f4-4261-b27f-e9aca43ee1d6/save-as-manual-food` on staging deployment `f522fae3` and returned HTTP 422 `incomplete_nutrition`/unsupported reusable snapshot behavior; the UI only displayed a generic failure alert. | P1 | Mobile Food Library eligibility predicate | The client excluded only recipe, mixed-meal, and AI-estimate origins, while the server also requires a persisted nutrition override for non-user-custom FoodItem origins. The client therefore exposed an action the server is designed to reject. | `apps/mobile/src/lib/food-library-ui.test.ts` failed before the fix because the eligibility helper was absent, then passed after asserting an unoverridden `food_item`/external-provider snapshot is not reusable. | `d34e310` | After the focused fix, Metro hot reload and the real QA A Simulator History re-observation showed provider Apple rows with no Save to My Foods action; weight, hydration, and History state remained intact. Full Saved/My Foods/Recent/Archived consumer coverage remains a separate sweep item. | FIXED |
| AI-MULTI-001 | Clear multi-food text review appeared to omit a named component | An initial QA A observation recorded only the first viewport of the review and was therefore insufficient evidence. On the resumed QA A Simulator run, the authenticated `/api/v1/ai/food-parse` response for `2 eggs, toast, banana` contained three items (`eggs`, `toast`, `banana`) with matched/review dispositions; the real review UI showed eggs and toast initially, then showed the banana row after scrolling the full review container. A second authenticated API boundary call for `chicken rice and peas` returned three items (`chicken`, `rice`, `peas`). Nothing was saved. | P1 (initially suspected) | Evidence/viewport interpretation (not a product defect) | The apparent omission came from treating a partial viewport observation as a complete review-state observation. No API or UI mapping defect was reproduced after checking the full scrollable review and the response boundary. | No code regression added; retain the named corpus in the acceptance matrix and require full-scroll plus response-boundary evidence for future AI multi-food checks. | — | Resolved as not reproduced. Full-scroll QA A Simulator evidence and response-boundary evidence agree on all three components; keep broader AI fallback/recoverability rows active. | CLOSED-NOT-REPRODUCED |
| PHOTO-SERVING-001 | Photo review nutrition preview disappeared after serving edit | QA A real Simulator photo flow reached review with an excluded recognition row and a trusted Apple row. The initial row showed a provisional nutrition preview; after editing the trusted serving amount (100 g → 50 g, then selecting a listed serving), the review UI kept the editable controls but showed only the `Provisional preview` heading with no kcal/resolved values. Continue to save still reached confirmation with a recalculated `122 kcal preview`; no save was performed. | P2 | Mobile photo-review serving-preview state | Serving-state mutation was initially suspected to clear the review preview without recomputing/rendering it until confirmation. A Node 22 source probe of `photoRowServingPreview` and `changePhotoServingChoice` returned exact/converted nutrition after gram and ounce edits. A controlled QA A Simulator rerun selected a real library image, replaced an unresolved row with trusted `Apple, raw`, entered 100 g, observed `61 kcal · 0.2 g protein` and `100.0 g resolved`, changed the unit to oz (3.53 oz), and observed the same `61 kcal` preview remain visible before reaching confirmation with the matching 61 kcal preview. | No code fix or new test; retain the named scenario in the final critical re-sweep and investigate again only if the discrepancy recurs with a controlled trace. | — | Controlled real-photo rerun did not reproduce the disappearance; preview remained authoritative through gram→ounce edit and matched confirmation. Keep physical camera/library coverage separate. | CLOSED-NOT-REPRODUCED |
| NAV-SAVED-VIEWS-001 | Saved Views route had no reachable back action | QA A real iOS 27 Simulator opened Explore trends → Manage saved views. The route displayed `‹ Explore trends` as plain text while root headers are hidden; repeated semantic and visible-coordinate attempts could not return to Explore trends. Source inspection confirmed the label was an `AppText` with no handler. | P1 | Mobile Trends navigation | The modal route omitted a Pressable back control, leaving users dependent on an unavailable system header/gesture and preventing normal recovery to Insights. | `apps/mobile/src/app/trends/__tests__/saved-views.test.tsx` now asserts a `Back to Insights` button calls `router.back`; red before the fix and green after it. | `d4859b4` | Focused Jest suite passes 9/9; Metro hot reload on the existing QA A Simulator exposed the new `Back to Insights` target, and tapping it returned to Explore trends. | FIXED-SIMULATOR |
| ANALYTICS-SAVED-VIEW-001 | Updating a renamed saved view reset its custom name | QA A renamed the pinned `Calories · 30D` view to `Calories QA`, relaunched, opened it, changed its comparison configuration, chose Save → Update existing view, and observed the list name revert to `Calories · 30D`. The modified Save View screen initialized its name from the generated metric/period label instead of the server's existing saved-view name. | P2 | Mobile saved-view edit/persistence boundary | `SaveViewScreen` did not load the existing saved-view record when editing by `savedViewId`; update payloads therefore overwrote a user name with the generated default. | `apps/mobile/src/app/trends/__tests__/save-view.test.tsx` adds a red/green regression that mocks a custom existing name and asserts the update preserves it. | `15bad81` | Focused save-view/saved-views/trend-detail suites pass; after the fix the real QA A Simulator showed `Calories QA` on the modified Save View screen and retained `Calories QA` after Update existing. | FIXED-SIMULATOR |
| ANALYTICS-COMPARE-001 | Calories comparison route omitted the selected comparison report | QA A opened the renamed saved view, selected a compatible comparison, applied it, and observed a `Compare` route whose accessibility tree contained only Calories summary/chart content and no comparison metric/chart. Source tracing showed `TrendDetailScreen` routed all Calories responses through `CaloriesReport`, bypassing the already-supported `comparisonChart`/`ComparisonTrendReport` branch. | P1 | Mobile analytics trend rendering | Special-case Calories/Weight branches ran before the shared comparison renderer, so a valid backend comparison response was silently discarded from the visible report. | `apps/mobile/src/app/trends/__tests__/trend-detail.test.tsx` adds a red/green Calories comparison fixture asserting `comparison-trend-report` renders and `calories-report` does not. | `15bad81` | Focused trend-detail suite passes; Metro-reloaded QA A Simulator now exposes `Calories + Sugar`, dual-axis copy, both metric legends, and a `Calories and Sugar comparison` accessibility target on the Compare route. | FIXED-SIMULATOR |
| MIX-UI-001 | Apparent valid mixed-meal preview failure was caused by an empty required meal name | Fresh QA A Simulator reproduction selected `Apple, Canada, flesh without skin, raw` and confirmed `100 g`; a temporary local-only probe observed `servingStatus=ready`, `amount=100`, `unit=g`, `ingredients=1`, but `name.length=0`. The visible `Rice bowl` text was only the `Meal name` placeholder. Accordingly `mixedMealPreviewRequest` correctly returned null. Entering the explicit name `QA Apple Bowl` changed the helper result to valid and the real UI displayed the authoritative `52 kcal` preview. The meal then logged once; two rapid confirm taps produced one persisted `QA Apple Bowl` row and the Progress total changed from 3 to 4 logs. | P1 (initially suspected) | Evidence/setup interpretation (not a product defect) | The earlier journey treated a placeholder as a persisted meal name and therefore exercised the intentional required-name gate. No serving handoff or preview-state loss was reproduced with a non-empty name. | No code regression added; retain a named regression requiring explicit meal-name entry before preview/save and assert that a placeholder alone does not count. | — | Resolved as not reproduced. The valid named flow produced preview and one persisted result under rapid double-submit; direct edit entry opened from Progress. A separate full History-tab re-observation remains part of the broader final re-sweep. | CLOSED-NOT-REPRODUCED |

## Execution gates and external dependencies (not product defects)

| ID | Checkpoint | Evidence | Impact | Next action |
| --- | --- | --- | --- | --- |
| ENV-SIM-001 | iOS Simulator UI harness | The host-disk blocker was cleared without repository or protected-state changes. On fresh iOS 27 simulator `A716FD01-3A7D-4D5C-90D7-71C139F9EAD1`, the approved Xcode beta workspace build completed with `** BUILD SUCCEEDED **`, produced a valid `FoodTracker.app`/`Info.plist`, installed, and launched. XcodeBuildMCP `snapshot_ui` now captures the accessibility hierarchy. The development client’s custom-scheme confirmation does not respond to XcodeBuildMCP tap/touch/key actions. A separate already-booted non-QA iPhone 17 Pro Max accepted the existing bundle and the supported Expo `--initialUrl` launch argument; after clearing only its Expo dev-menu onboarding preference, real host-window interaction plus XcodeBuildMCP snapshots reached the signed-out screen, password-reset screen, sign-in return path, and create-account empty-form validation. A second Metro session with `APP_ENV=staging` and the verified Railway API target reached the signed-out screen and produced the matching staging `401` request. | Native install/launch/accessibility setup and signed-out local/staging navigation journeys are evidenced on the non-QA Simulator. XcodeBuildMCP element actions still report success without reliably changing some React Native targets on iOS 27, so host-window interaction is the evidence path for reachable signed-out controls; dedicated QA A/B identities now permit authenticated and non-destructive mutation journeys, while QA C remains deleted and unavailable. | Continue using the supported `--initialUrl` path for reachable journeys; never erase/reset either Food Tracker QA simulator or use QA C for unrelated regression. |
| ENV-STAGE-001 | Railway staging provenance | RESOLVED for API and staging-target Simulator evidence. The first upload created `b0d04d9b-c9d2-414f-94d3-647007339f59` but stayed `INITIALIZING` without a build. A clean tracked `git archive` of exact candidate `14c0472c1aeb59abe75e4f0cf8507dd70e74a98a` was retried against the existing `food-tracker-staging-api` staging service, creating `f522fae3-825e-425c-810d-344fc850db6d`. Railway built the workspace, ran Prisma generation and API/shared build, ran readiness migrations with no pending migrations, and marked the deployment `SUCCESS`; image digest is `sha256:280dc90401530e212496d65d4b76a649653cb863fc36067f8570314a95d89f32`. Railway upload metadata has no commit hash, so source provenance is the exact archive command/candidate SHA plus deployment ID rather than an inferred Git field. The first API-only sleep probe logged `Stopping Container` after more than ten minutes idle and `/health/ready` returned 200 in 5.490 seconds. A second idle run logged `Stopping Container` at `13:02:08`; the staging-target Simulator launch produced the first post-sleep `/api/v1/setup/status` request at `13:02:58`, the container restarted, and the real UI snapshot reached signed-out `Welcome back`. The guarded seed then ran inside the deployed service via Railway SSH for QA A with UID/email cross-check and anchor `2026-09-10`, producing 560 food logs, 6,754 nutrient snapshots, 106 weights, 485 water logs, four saved views (one pinned), and two recommendations. | Staging source provenance, API cold-start behavior, signed-out staging-target Simulator cold launch, and deterministic QA A fixture state are evidenced for deployment `f522fae3-825e-425c-810d-344fc850db6d`. No production service or data was touched. Authenticated cold launch remains gated by host UI access. | Preserve the deployment ID, source SHA, seed report, timestamps, HTTP logs, Metro target evidence, and Simulator snapshot in closeout; do not infer authenticated staging UI behavior without observed Simulator interaction. |
| ENV-STAGE-002 | Current-HEAD staging reprovenance | The exact clean archive of candidate `17b85b01d56e6a0f44d4c9eb3425370d82a8fa9d` created deployment `3bbfb81d-09ce-4bae-86ee-c875f6efe400`; Railway later marked it `FAILED` with config error `Failed to create code snapshot. Please review your last commit, or try again.` A later authorized clean-archive retry for committed candidate `42b4d5b` used documented `--path-as-root`, indexed and uploaded successfully, then was rejected because free-tier SFO deploys were unavailable during the 8 AM–8 PM America/Los_Angeles peak window. The subsequent authorized exact archive of candidate `27bde59` created deployment `0e96cd56-8193-4263-9c57-b2babbbf9fff`, which reached terminal `FAILED` at `2026-09-13T04:05:43.201Z` with zero build/deploy logs. A further authorized exact archive of current committed tip `8f039c4` created deployment `5865a859-b33a-49ea-b465-0cb343830821`; Railway now reports it terminal `FAILED` at `2026-09-13T04:29:26.894Z`, again with zero build/deploy logs. No current candidate has produced served-commit metadata. `/health` continues to reflect only the previously served runtime and is not treated as provenance. | The prior deployment `f522fae3` remains the only verified runtime evidence for API candidate `14c0472`. Candidates `17b85b0`, `42b4d5b`, `27bde59`, and `8f039c4` have exact local archive provenance but no verified served revision or current-HEAD staging UAT; no behavior is inferred from their health responses or failed deployment attempts. | When Railway availability permits a later validated candidate, inspect build/deploy logs and provenance and only then run current-candidate staging/cold-start acceptance. Continue independent Simulator/API work meanwhile. |
| ENV-AUTH-001 | Dedicated QA identities | Authorized Firebase-linked QA A and B identities authenticate against the configured Firebase project and match their approved UID pairs. Read-only calls to the existing staging API returned 200 for both accounts across setup status, profile, goals, dashboard summary, food logs, water logs, weight logs, and recommendations. The guarded QA A fixture is seeded at anchor `2026-09-10` with 560 food logs, 6,754 nutrient snapshots, 106 weights, 485 water logs, four saved views (one pinned), and two recommendations; QA B remains independently empty across those collections. Read-only ownership probes confirmed QA B receives 404 for QA A food-log, saved-view, water-log, weight-log, and recommendation IDs. Fresh beta-built iOS 27 Simulator interaction observed QA A sign-in/relaunch, QA A sign-out, QA B sign-in, empty QA B Progress/History/Insights, and QA B relaunch back to its authenticated home. The disposable C credential pair was independently verified against Firebase and returned the approved UID before the authorized deletion; C is now deleted and routed to signed-out state. | A/B identity, server mapping, deterministic QA A fixture, API ownership isolation, observed QA A→B UI switching, and C-only deletion are resolved. Preserve A/B and everyday data; never substitute another account for destructive tests. | Continue authenticated A/B regression and final re-sweep; no further destructive account action is authorized. |

| ENV-SIM-002 | Simulator host interaction | The host session is now unlocked and the approved per-command Xcode beta build completed with `** BUILD SUCCEEDED **`. The exact app bundle was installed on QA A simulator `53D0A189-7A75-49B1-97E6-A4C5DC4CB12F`; real host-window interaction entered QA A credentials, submitted sign-in, observed the authenticated Complex Progress UI, terminated/relaunched the app, and observed the authenticated home again. The same real interaction signed QA A out, signed QA B in, observed empty Progress/History/Insights state, and returned to the authenticated QA B home after relaunch. The iOS Save Password prompt appeared as a system overlay and was dismissed without saving credentials. XcodeBuildMCP actions still require post-action UI observation; action-return status alone is not evidence. | QA A and QA B authenticated switching/relaunch are now evidenced. The `1 apple` flow and remaining product journeys still require their own observed UI evidence. | Continue the remaining authenticated journeys using fresh snapshots and observed state; do not infer PASS from build or tool-return status. |
| ENV-AUTH-002 | Disposable QA C authentication | Direct Firebase sign-in verification for the corrected disposable C email/UID pair returned HTTP 200 and the approved UID `EK8NrXgXlBVZfaABnnDtXgK34tn2`. QA C was freshly signed in through the real Simulator and its independently recognizable profile (`testacct4659`) was observed. The alias and UID were reverified immediately before the authorized destructive action; current-password reauthentication completed deletion and signed the app out. | C identity verification, recent-auth recovery, permanent deletion, and post-delete signed-out routing are resolved. QA C must not be recreated or reused for unrelated regression. | Preserve deletion evidence and continue only with non-destructive A/B work. Never substitute A/B or an everyday account. |
| ENV-DATA-001 | Seeded historical nutrition evidence | Read-only QA A staging retrieval returned 560 FoodLogs with 6,754 sparse nutrient observations. The fixture contains one explicit numeric zero; unprovided nutrients remain absent from each sparse nutrient object (12 or 13 keys per log), with no null-to-zero coercion observed. Real QA A Complex History edit/reopen evidence also preserved numeric, explicit-zero, and blank/Unknown states without changing the reusable FoodItem. | Backend snapshot shape and Unknown-versus-zero semantics are evidenced at API and Simulator boundaries. | Retain the seeded fixture and repeat only during the final critical re-sweep if a later code change could affect snapshot semantics. |
| ENV-WATER-001 | Hydration persistence and cleanup | Against the seeded staging API, QA A created a temporary 250 mL water log, read it back with the same amount, deleted it, and received `404` on a post-delete read. Real QA A Simulator coverage opened Log Water, used Other Amount to add 250 mL, observed the persisted local-day row and hydration total, and opened the editable water History form with date/time and delete controls. | API create/read/delete persistence and the primary Complex UI entry/edit surface are evidenced; Undo semantics remain limited to the currently implemented design and any final re-sweep check. | Retain the reversible water row only as QA fixture state; repeat only if a later change affects hydration persistence or editing. |
| ENV-WEIGHT-001 | Weight persistence, update, cleanup, and ownership | Against the seeded staging API, QA A was verified by Firebase UID, then a temporary weight log was created at 181.2 lb, read back at 181.2 lb, updated to 181.5 lb, read back at 181.5 lb, deleted, and returned `404` on a post-delete read. A QA A seeded weight-log ID was then requested with QA B's verified token and returned `404 Weight log not found`. Real QA A Simulator Progress/History journeys also exercised weight trend access and local-day seeded values. | API create/read/update/delete, server-side ownership isolation, and representative weight/progress UI are evidenced. | Retain the ownership probes and repeat only during final re-sweep if later changes affect weight or analytics propagation. |
| ENV-GOAL-001 | Goal/target read-only contract snapshot | On the verified QA A staging identity, `GET /goals` returned the seeded Maintain plan with `goalType=maintain`, `goalPace=null`, `targetRateLbPerWeek=null`, and numeric calorie/protein/carbohydrate/fat targets. No mutation was made. QA A Simulator Profile evidence also showed the persisted Lose plan/rate, target summary, Goal Plan editor, and Nutrition Targets recommended/custom/reset flows. | Maintain null-rate, numeric Goal Pace, effective-target hierarchy, override/reset, and representative downstream propagation are evidenced at API and Simulator boundaries. | Preserve the current seeded plan; repeat only if a later change affects goal or target propagation. |
| ENV-ANALYTICS-001 | Analytics/Insights read-only seeded response | On the verified QA A staging identity, advanced analytics for `2026-09-10`, `America/Toronto`, and a 30-day range returned calorie/protein trend objects, weight-trend fields, data-completeness metadata, and nutrient coverage fields. Monthly Insights returned eight implemented sections (`calories`, `protein`, `carbs`, `fat`, `macroComposition`, `weight`, `hydration`, `loggingConsistency`); streak calendar returned five weeks; daily nutrients returned 20 nutrient keys. QA A Simulator evidence exercised week/month reports, Explore trends, saved-view management, custom/compare routes, forecast-ineligible messaging, and persistence across relaunch. | Response-shape, sparse-data, deterministic backend analytics, forecast eligibility, source-to-screen conclusions, filters, comparisons, and saved-view interactions are evidenced at API and Simulator boundaries; visual chart polish remains Phase 24 scope. | Repeat only during the final critical re-sweep or if a later change affects analytics state/calculation. |

## Inspection findings and dispositions

| ID | Area | Finding | Required proof before remediation |
| --- | --- | --- | --- |
| AI-ARCH-HYPOTHESIS-001 | AI text logging | The original planning hypothesis was proven and fixed at the request boundary; the current source now contains bounded retrieval plus adequacy evaluation. Follow-up response-boundary and complete-scroll Simulator evidence preserved all named components for the exercised multi-food cases, so the separate completeness concern was closed as not reproduced. | API request-boundary coverage proves bounded evidence evaluation, trusted/review decisions, and invalid/incomplete-decision rejection. Real Simulator evidence covers `1 apple`, multi-food trusted/review rows, partial unresolved components, and opaque serving-required review. Keep the named corpus active for final re-sweep without treating the original architectural defect as open. |

## Latest automated validation checkpoint

The latest committed-source validation rerun completed under Node `v22.23.0`
and pnpm `10.34.3` after `b077624`. Docker PostgreSQL was restarted
non-destructively; the dedicated test database accepted the connection and
the full API suite completed successfully.

- API: 116 test files / 1,401 tests passed.
- Mobile Jest: 70 suites / 211 tests passed.
- Mobile Vitest: 67 files / 444 tests passed.
- Lint, typecheck, workspace build, Prisma generate/validate, and test-database
  migration status/deploy passed.
- Changed-file Prettier checks and `git diff --check` passed.
- Root `format:check` exits 1 on 27 pre-existing protected/local files only:
  one `.agents` skill, 24 `.superpowers/sdd` records,
  `apps/api/food_search_diagnostic.mjs`, and the unchanged
  `apps/mobile/src/app/trends/__tests__/saved-views.test.tsx`; all changed files pass targeted
  Prettier checks and none of those files was edited.
- Current-HEAD Railway staging upload `b4b203be` was recorded as `SKIPPED`
  because no watched runtime files changed; identity/device acceptance remain
  external gates and are not inferred from these automated results.

The older per-defect validation prose above may retain historical test counts
from the checkpoint at which that defect was fixed. The superseding current
validation is 67 Vitest files / 444 tests and 70 Jest suites / 211 tests.

## Deferred visual-only findings

Record spacing, typography, color, hierarchy, card, animation, and chart
aesthetic findings here or in linked Phase 24 issues. Do not mix them with
functional defects. The rule is: ugly is Phase 24; unusable is debugging.
