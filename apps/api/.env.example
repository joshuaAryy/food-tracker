PORT=3000
# Local development placeholder. A real running PostgreSQL database is required before migrations or persistence.
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/food_tracker"

# AI providers are backend-only. Do not expose Gemini keys to mobile clients.
AI_PROVIDER=disabled
GEMINI_API_KEY=
GEMINI_FOOD_PARSE_MODEL=gemini-2.5-flash
AI_FOOD_PARSE_MAX_DESCRIPTION_CHARS=500
AI_FOOD_PARSE_MAX_ITEMS=8
AI_FOOD_PARSE_TIMEOUT_MS=8000
AI_FOOD_PARSE_RATE_LIMIT_WINDOW=600000
AI_FOOD_PARSE_RATE_LIMIT_MAX=5
AI_FOOD_PARSE_DAILY_LIMIT=25

# Photo analysis is backend-only. Images are processed in memory and never retained.
GEMINI_PHOTO_ANALYSIS_MODEL=gemini-2.5-flash
PHOTO_ANALYSIS_MAX_ITEMS=8
# Supports the maximum eight-item structured extraction response; bounded to 2048-4096.
PHOTO_ANALYSIS_MAX_OUTPUT_TOKENS=2048
PHOTO_ANALYSIS_TIMEOUT_MS=15000
PHOTO_ANALYSIS_RATE_LIMIT_WINDOW_MS=600000
PHOTO_ANALYSIS_RATE_LIMIT_MAX=5
PHOTO_ANALYSIS_DAILY_LIMIT=25
# Optional bounded text-only candidate adjudication. Disabled by default.
PHOTO_CANDIDATE_ADJUDICATION_ENABLED=false
PHOTO_NUTRITION_ESTIMATION_ENABLED=false
PHOTO_NUTRITION_ESTIMATION_MOCK=missing
# Mixed trusted/estimated confirmation is disabled by default. Proofs are signed with HMAC-SHA-256.
PHOTO_ESTIMATE_CONFIRMATION_ENABLED=false
PHOTO_ESTIMATE_PROOF_SECRET=
PHOTO_ESTIMATE_PROOF_TTL_SECONDS=900
# Kept short so recognition, retrieval, and adjudication fit the mobile budget.
# Expanded B2 resolution assistance remains below the 15-second photo budget.
PHOTO_CANDIDATE_ADJUDICATION_TIMEOUT_MS=2500
PHOTO_CANDIDATE_ADJUDICATION_MAX_CANDIDATES=3
PHOTO_CANDIDATE_ADJUDICATION_MAX_ROWS=8
PHOTO_CANDIDATE_ADJUDICATION_MAX_OUTPUT_TOKENS=1024

# Generic food nutrition lookup is backend-only. Do not expose USDA keys to mobile clients.
USDA_FDC_API_KEY=
USDA_FDC_BASE_URL=https://api.nal.usda.gov/fdc/v1
USDA_FDC_TIMEOUT_MS=5000
USDA_FDC_SEARCH_LIMIT=3
USDA_FDC_RATE_LIMIT_WINDOW=600000
USDA_FDC_RATE_LIMIT_MAX=20
